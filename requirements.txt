# Core runtime deps
MetaTrader5>=5.0
pandas>=2.0
numpy>=1.24
python-dateutil>=2.9

# Notes:
# - Tkinter is part of the Python standard library; no pip package is required.
# - The MT5 Manager SDK ("MT5Manager") is provided by MetaQuotes/vendor and is not
#   installable via pip. Ensure it is available on PYTHONPATH when running.
